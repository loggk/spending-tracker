# Copy to .env and fill in with values from `cdk deploy` stack outputs.
VITE_AWS_REGION=us-east-1
VITE_USER_POOL_ID=
VITE_USER_POOL_CLIENT_ID=
# Only used by the dev server. A deployed build calls the API through CloudFront
# on its own origin, so it ignores this.
VITE_API_URL=
